name: CI Pipeline

on: [push]

jobs:
  deploy-ansible:
    runs-on: self-hosted

    steps:

    - name: ping
      run: ansible-playbook -i /home/ansible/devops/hosts /home/ansible/devops/ping.yml --ask-vault-pass
